name: coder_mcp
description: Model Context Protocol client, transports, and wire types.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  http: ^1.5.0
  process: ^5.0.5

dev_dependencies:
  fake_async: ^1.3.3
  mocktail: ^1.0.4
  test: ^1.26.3
