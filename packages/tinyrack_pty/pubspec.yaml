name: tinyrack_pty
description: Private cross-platform pseudo-terminal processes for Tinyrack Coder.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

platforms:
  linux:
  macos:
  windows:

dependencies:
  code_assets: ^1.2.1
  ffi: ^2.2.0
  hooks: ^2.0.2
  native_toolchain_c: ^0.19.2

dev_dependencies:
  test: ^1.31.0
