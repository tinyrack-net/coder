name: coder_protocol
description: Versioned protocol and immutable DTOs for Tinyrack Coder.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0

dev_dependencies:
  build_runner: 2.12.2
  freezed: ^3.2.5
  json_serializable: ^6.14.1
  test: ^1.26.3
