name: relay
description: Opaque WebSocket relay server for Tinyrack Coder.
version: 0.2.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  relay_protocol: ^0.1.0
  shelf: ^1.4.2
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  test: ^1.26.3
